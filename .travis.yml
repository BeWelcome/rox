language: php

php:
  - '7.0'

addons:
  mariadb: '10.1'

cache:
  directories:
    - $HOME/.composer/cache
    - $HOME/.npm
    - $HOME/.nvm

before_install:
  - nvm install v5.5
  - nvm use v5.5
  - gem install sass
  - phpenv config-rm xdebug.ini

install:
  - composer install --no-interaction --ignore-platform-reqs
  - npm install

before_script:
  - mysql -e 'create database bewelcome;'
  - mysql -e 'GRANT ALL PRIVILEGES ON bewelcome.* TO \'bewelcome\'@\'localhost\' IDENTIFIED BY \'bewelcome\'';
  - mysql -e 'FLUSH PRIVILEGES;'

script:
  - ./vendor/bin/phinx seed:run
  - make && make build
