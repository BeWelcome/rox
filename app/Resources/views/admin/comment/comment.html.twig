{% extends 'base.html.twig' %}
{% import 'macros.twig' as macros %}

{% block content %}
    <h1>{{ 'AdminComment' | trans }}</h1>
    <div class="row">
        <div class="col-12 mb-2">
            {% if comment.displayinpublic %}{{ 'Comment is visible to everyone.' }}{% else %}{{ 'Comment is currently hidden.' }}{% endif %}
        </div>
        <div class="col-12 mb-2">
            {% if comment.allowedit %}{{ 'Comment can be edited.' }}{% else %}{{ 'Comment is currently locked.' }}{% endif %}
        </div>
    </div>
    <div class="row">
        {% include ':member:comment.html.twig' with { 'comment': comment } %}
        {% if reply %}
            {% include ':member:comment.html.twig' with { 'comment': reply } %}
        {% endif %}
        <div class="col-12 col-sm-6 card">
            {{ form(form, { 'attr': { 'class': 'form-inline'}}) }}
        </div>
    </div>
{% endblock %}