{% extends ':emails:email.html.twig' %}

{% block content%}
    Sender: {{ sender.username }}
    {{ message_text | raw }}

    <div id="footer">
        <p>(cc) {{ "now" | date("Y") }} BeWelcome</p>
    </div>
{% endblock %}