{% extends 'base.html.twig' %}

{% block title %}
    {{ 'Wiki' | trans }}
{% endblock %}

{% block content %}

{# Wiki form to create page content... #}
{# Use CKEditor and map html to Wiki content #}
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <div><input class="btn btn-primary form-control" type="submit" value="{{ 'Create' | trans }}" ></div>
    {{ form_end(form) }}
{% endblock content %}
