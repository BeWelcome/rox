{# TODO: make a wordcode for this text #}

<div class="row">
    <div class="col-12 col-lg-6 mb-3">
        <h1 class="m-0">{{ 'signup.finished' | trans }}</h1>
        <p>{{ 'signup.finished.congratulations'|trans({'username': username}) }}</p>
        <p>{{ 'signup.finished.sent.to'|trans({'email': email_address}) }}</p>
        <p>{{ 'signup.finished.check.spam'|trans }}</p>
    </div>

    <div class="w-100"></div>

    {# TODO: add useful info #}
    <div class="col-12 col-lg-6">
        <a href="{{ url('landingpage') }}" class="btn btn-primary">BeWelcome</a>
    </div>

</div>
