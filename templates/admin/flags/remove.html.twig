{% extends 'base.html.twig' %}

{% block content %}
  <h1 class="u:text-24">{{ 'AdminFlagsRemove'|trans }}</h1>
  <p>{{ 'admin.flags.remove.confirm'|trans({
    '%flag%': assignment.flag.name,
    '%username%': assignment.member.username
  }) }}</p>
  <dl>
    <dt>{{ 'AdminFlagsLevel'|trans }}</dt>
    <dd>{{ assignment.level }}</dd>
    <dt>{{ 'AdminFlagsScope'|trans }}</dt>
    <dd>{{ assignment.scope }}</dd>
    <dt>{{ 'AdminFlagsComment'|trans }}</dt>
    <dd>{{ assignment.comment|nl2br }}</dd>
  </dl>
  {{ form(form) }}
{% endblock content %}
