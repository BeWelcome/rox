{% extends 'emails/email.html.twig' %}

{% block content %}
    <p>
        {{ 'group.accepted.invitation'|trans({'username': invitee.Username, 'group': '<a href="' ~ url('group_start', {'group_id': group.id}) ~ '">' ~ group.Name ~ '</a>'})|raw }}
    </p>
{% endblock content %}
