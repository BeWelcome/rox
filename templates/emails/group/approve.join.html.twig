{% extends 'emails/email.html.twig' %}

{% block content %}
    <p>
        {{ 'group.join.approve'| trans({'admin': admin.Username, 'group': '<a href="' ~ url('group_start', {'groupId': group.id}) ~ '">' ~ group.Name ~ '</a>'} %}) }}
    </p>
{% endblock content %}
