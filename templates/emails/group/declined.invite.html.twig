{% extends 'emails/email.html.twig' %}

{% block content %}
    <p>
        {{ 'group.declined.invitation'|trans({'username': invitee.Username, 'groupname': '<a href="' ~ url('group_start', {'group_id': group.id}) ~ '">' ~ group.Name ~ '</a>'})|raw }}
    </p>
{% endblock content %}
