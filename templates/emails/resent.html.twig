{% extends 'emails/email.html.twig' %}

{% block content%}
    <p>{{ 'signup.resent.sorry'|trans }}<br>
       {{ 'signup.resent.reason'|trans }}</p>
    <p>{{ 'signup.confirm.link'|trans({'email_address': email_address}) }}<p>
    <p><a href="{{ url('signup_confirm', { username: username, registrationKey: key }) }}"></a>{{ 'signup.confirm.email.address'|trans }}</p>
{% endblock content %}
