{% block content %}
<div id="legsdisplay">
    <div class="c-trip-legs--landing">
        {% for leg in legs  %}
            {% include 'trip/landing.html.twig' with {'leg': leg} %}
        {% else %}
                {{ 'landing.bevisited.none'|trans }}
        {% endfor %}
    </div>
</div>
{% endblock %}
