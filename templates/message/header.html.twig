{% import 'macros.twig' as macros %}
<div class="row no-gutters sticky-top py-1 bg-white mb-1 px-1 justify-content-between">
    <div>{{ macros.roundedavatarstack(otherUser.Username, 50, false) }}</div>
    <div>
        <h3 id="MessageSubject" class="u-rounded-4 u-p-4 text-truncate">{{ currentSubject }}</h3>
    </div>
    <div class="text-right">{{ macros.roundedavatarstack(myself.Username, 50, true) }}</div>
</div>
