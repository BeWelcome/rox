{% extends 'profile/profile.html.twig' %}

{% block content %}
    {% if own %}
        Your own profile
    {% else %}
        {{ member.Username }}'s profile
    {% endif %}
    {% dump(member) %}
    {{ dump(member.translatedFields) }}
    {{ dump(member.translatedFields.toArray) }}
    {{ dump(member.fields) }}
{% endblock %}
