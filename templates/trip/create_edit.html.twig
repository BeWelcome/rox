{% extends 'trips.html.twig' %}

{% block title %}
  {{ 'trip.create' | trans }}
{% endblock %}

{% block stylesheets %}
  {{ encore_entry_link_tags('tailwind') }}
  {{ encore_entry_link_tags('jquery_ui') }}
  {{ encore_entry_link_tags('trips')  }}
{% endblock stylesheets %}

{% block javascripts %}
  {{ encore_entry_script_tags('tempusdominus') }}
  {{ encore_entry_script_tags('trips')  }}
{% endblock javascripts %}

{% macro widget_prototype(widget) %}
  {% if widget.vars.prototype is defined %}
    {% set form = widget.vars.prototype %}
    {% set name = widget.vars.name %}
  {% else %}
    {% set form = widget %}
    {% set name = widget.vars.full_name %}
  {% endif %}

  <div data-content="{{ name }}" class="c-trips__info">
    <div>
      {{ form_label(form.location.name) }}
      {{ form_widget(form.location.name, {'attr': { 'class': 'o-input--xl'}}) }}
    </div>
    <div>
      {{ form_label(form.duration, null, {'label_attr': { 'class': 'o-label o-label--white'}}) }}
      {{ form_widget(form.duration, {'attr': { 'class': 'o-input--xl'}}) }}
    </div>
    <button class="o-rounded" data-related="{{ name }}">
      <i class="fa fa-minus"></i>
    </button>
    {{ form_widget(form.options[0]) }}
    {{ form_widget(form.options[1]) }}
    {{ form_widget(form.location.geoname_id) }}{{ form_widget(form.location.latitude) }}{{ form_widget(form.location.longitude) }}
    {{ form_widget(form.arrival) }}{{ form_widget(form.departure) }}
  </div>
{% endmacro %}

{% block content %}
  {% apply spaceless %}
    {% set button_text %}
      {% if create %}{{ 'trip.create' | trans }}{% else %}{{ 'trip.edit' | trans }}{% endif %}
    {% endset %}
  {% endapply %}
  <h1 class="u-text-56 u-leading-56 u-font-700 u-mb-24 u-text-white">{% if create %}{{ 'trip.create' | trans }}{% else %}{{ 'trip.edit' | trans }}{% endif %}</h1>

  {{ form_start(form) }}
  <div>
    <div class="c-trips__title">
      {{ form_row(form.summary, {'attr': { 'class': 'o-form-group--white'}}) }}
    </div>
    <div class="c-trips__description">
      {{ form_row(form.description, { 'attr': { 'rows': 5 }}) }}
    </div>
    <div class="u-flex">
      {{ form_row(form.countoftravellers) }}
      {{ form_row(form.additionalinfo) }}
    </div>
    <div id="subtrips-list"
       data-prototype="{{ _self.widget_prototype(form.subtrips.vars.prototype)|escape }}">
      {% for subtrip in form.subtrips %}
        {{ _self.widget_prototype(subtrip) }}
      {% endfor %}
    </div>
    <button class="o-rounded" data-target="subtrips-list"><i class="fa fa-plus"></i></button>
    <div class="d-flex col-12 justify-content-end">
      <input class="btn btn-primary" type="submit" value="{{ button_text|trans|capitalize }}">
    </div>
  </div>
  {{ form_end(form) }}
{% endblock content %}
