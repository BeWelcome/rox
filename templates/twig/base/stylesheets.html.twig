{% for stylesheet in stylesheets %}
    {% if stylesheet starts with '/' %}
        <link rel="stylesheet" type="text/css" href="{{ stylesheet }}">
    {% else %}
        <link rel="stylesheet" type="text/css" href="/styles/css/{{ stylesheet }}">
    {% endif %}
{% endfor %}