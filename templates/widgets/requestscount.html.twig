<div id="requestCount" data-count="{{ requestCount }}">
    <a href="{{ path('requests') }}" class="nav-link">
        <i class="fa fa-lg fa-bed"></i>&nbsp;
        <span class="badge badge-warning d-md-none d-xl-inline-block">{{ requestCount }}</span>
    </a>
</div>